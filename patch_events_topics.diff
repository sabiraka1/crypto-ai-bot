diff --git a/src/crypto_ai_bot/core/infrastructure/events_topics.py b/src/crypto_ai_bot/core/infrastructure/events_topics.py
index 0000000..0000001 100644
--- a/src/crypto_ai_bot/core/infrastructure/events_topics.py
+++ b/src/crypto_ai_bot/core/infrastructure/events_topics.py
@@ -1,20 +1,18 @@
 from __future__ import annotations
 
 from datetime import datetime
 from decimal import Decimal
-from typing import Final, TypedDict, Optional, Literal
+from typing import Final, TypedDict, Optional, Literal, Any
 
-from crypto_ai_bot.utils.decimal import dec
-
@@
 class AlertPayload(TypedDict):
     """Alert event payload"""
     trace_id: str
     timestamp: str
     severity: Literal["info", "warning", "error", "critical"]
     title: str
     message: str
-    metadata: Optional[dict[str, any]]
+    metadata: Optional[dict[str, Any]]
 
@@
 def build_regime_event(
-    old_state: str,
-    new_state: str,
+    old_state: Literal["risk_on", "risk_small", "neutral", "risk_off"],
+    new_state: Literal["risk_on", "risk_small", "neutral", "risk_off"],
     score: float,
     dxy_change: Optional[float],
     btc_dom_change: Optional[float],
     fomc_active: bool,
     trace_id: str
 ) -> tuple[str, RegimePayload]:
