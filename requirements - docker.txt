# ============================================================================
# DOCKER OPTIMIZED REQUIREMENTS
# ============================================================================
# Оптимизированные зависимости для Docker контейнеров

# Включаем основные зависимости
-r requirements.txt

# ==== CONTAINER SPECIFIC ====
# Меньшие версии для уменьшения размера образа
matplotlib==3.9.2          # С бэкендом Agg для headless режима

# ==== SYSTEM INTEGRATION ====
supervisor==4.2.5          # Управление процессами в контейнере

# ==== HEALTHCHECK UTILITIES ====
healthcheck==1.3.3         # HTTP healthcheck эндпоинты

# Исключения для Docker (закомментированы в основном файле):
# plotly - слишком тяжелый для production
# kaleido - не нужен в headless режиме
# jupyter - не нужен в production