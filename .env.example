# === Runtime mode ===
# paper | live | backtest
MODE=paper

# === Symbols / timeframe ===
SYMBOL=BTC/USDT
TIMEFRAME=1h
LIMIT=300

# === Trading safety ===
ENABLE_TRADING=false        # для live: разрешить выставлять ордера
SAFE_MODE=true              # если true — блокирует торговлю в любом режиме

# === Exchange (для MODE=live через CCXT) ===
EXCHANGE_ID=binance
API_KEY=
API_SECRET=
API_PASSWORD=
API_SUBACCOUNT=

# === DB ===
DB_PATH=crypto.db

# === Risk / limits (примеры) ===
MAX_DRAWDOWN_PCT=0.2
STOP_LOSS_PCT=0.01
TAKE_PROFIT_PCT=0.02
MAX_SEQ_LOSSES=3
MAX_EXPOSURE=0.5

# === Decision weights ===
SCORE_RULE_WEIGHT=0.5
SCORE_AI_WEIGHT=0.5

# === Rate limits (use-cases) ===
EVALUATE_RATE_PER_MIN=60
ORDERS_RATE_PER_MIN=10

# === Time sync / drift ===
TIME_DRIFT_LIMIT_MS=1000
# Можно оставить пустым: дефолтные URL внутри utils/time_sync.py
TIME_DRIFT_URLS=

# === Telegram (если используете) ===
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
