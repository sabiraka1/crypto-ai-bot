# Подключение боевых зависимостей
-r requirements.txt

# TEST
pytest>=8.2,<9
pytest-asyncio>=0.23,<1
pytest-mock>=3.12,<4
pytest-cov>=4.1,<5
pytest-timeout>=2.2,<3
coverage>=7.4,<8

# LINT / STYLE / ARCH
ruff>=0.6,<0.7
mypy>=1.8,<2
import-linter>=2.0,<3
black>=24.0,<25
isort>=5.13,<6

# SECURITY (используем pip-audit вместо safety)
bandit>=1.7,<2
pip-audit>=2.6,<3
# safety удалена - конфликтует с pydantic

# TYPE STUBS
types-requests>=2.31,<3
types-redis>=4.6,<5

# DEV TOOLS
pre-commit>=3.6,<4
ipython>=8.18,<9