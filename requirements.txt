# ── CORE API ─────────────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.6
gunicorn==22.0.0           # для Railway: gunicorn c uvicorn-воркерами

# ── DATA / TRADING ───────────────────────────────────────────────────────────
numpy==1.26.4               # стабильная для многих билдов
pandas==2.2.2               # работа со свечами/индикаторами
ccxt==4.4.7                 # биржи (Gate.io/…)
pydantic==2.8.2             # настройки/валидация
python-dotenv==1.0.1        # загрузка .env на локалке/релизе

# ── METRICS / LOGGING (небольшие, но полезные) ───────────────────────────────
prometheus-client==0.21.0   # /metrics (опционально, но уже используется)
structlog==24.4.0           # удобные структурированные логи

# ── ТЕХАНАЛИЗ (опционально, включим позже при необходимости) ────────────────
# ta==0.11.0                # если захочется готовые индикаторы (сейчас считаем сами)
# scikit-learn==1.5.1       # если подключим ML-модель
# joblib==1.4.2             # сериализация моделей

# ── HTTP-клиенты (НЕ нужны: используем urllib в контексте) ───────────────────
# requests==2.32.3          # добавь только если реально потребуется
matplotlib>=3.8,<3.11
pillow>=10.0
joblib>=1.3
scikit-learn>=1.4
